<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/home">Home</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/enrolledCourse/<%= result.id %>">Your Enrolled Courses</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/premium/<%= result.id %>">Premium Membership</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav><br>

    <h1>Profile Page</h1>
    <img src="<%= result.UserProfile.profilePicUrl %>" alt="">
    <h2>
        <%= result.UserProfile.fullName() %>
    </h2>
    <p>User Id: <%= result.id %>
    </p>
    <p>Account Type: <%= result.Role.name %> (<%= result.Role.description %>)</p>
    <p>Email: <%= result.email %>
    </p>
    <p>Education: <%= result.UserProfile.education %>
    </p>
    <p>Date of Birth: <%= result.UserProfile.birthDate %>
    </p>
    <a href="/profile/<%= result.id %>/edit">Edit Profile</a>
    <br>
    <a href="/profile/<%= result.id %>/delete">Delete Account</a>
</body>

</html>